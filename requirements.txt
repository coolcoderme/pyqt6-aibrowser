# Runtime dependencies for browserdemo.py (PyQt6 chrome + Blink/Chromium engine)
PyQt6

# Google Chrome or Chromium is required on the system PATH (or set CHROME_PATH).
# The Python UI talks to it over the Chrome DevTools Protocol.

# Lint tooling (configured by .flake8 and .pylintrc)
flake8
pylint

# Optional: enables the in-app "Cursor SDK" agent provider.
# Uncomment to install. The app runs fully without it.
# cursor-sdk
